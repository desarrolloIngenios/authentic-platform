apiVersion: v1
kind: Service
metadata:
  name: moodle-elearning-service
  namespace: moodle-elearning
spec:
  selector:
    app: moodle-elearning
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
